# MVC Template with Typer

Project ini adalah aplikasi Python yang berjalan melalui command line (CLI).  
Command utama yang digunakan adalah:

```
python main.py engine run

python main.py kafka_consumer run

```